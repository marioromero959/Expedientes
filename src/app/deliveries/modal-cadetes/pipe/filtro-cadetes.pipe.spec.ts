import { FiltroCadetesPipe } from './filtro-cadetes.pipe';

describe('FiltroCadetesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCadetesPipe();
    expect(pipe).toBeTruthy();
  });
});
